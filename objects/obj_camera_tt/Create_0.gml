/// @description Variables

//view = view_camera[0];
xx = x;
yy = y;
target = obj_player_1;

camera_set_view_size(view_camera[0], 320, 180);

width = camera_get_view_width(view_camera[0]);
height = camera_get_view_height(view_camera[0]);

////Centre the screen
//window_center();

camera_set_view_size(view_camera[1], 0, 0);