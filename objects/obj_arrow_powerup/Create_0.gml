/// @desc

// Inherit the parent event
event_inherited();

mode = target_mode.object_self;
powerup_type = powerup.arrow;
powerup_time = 600;
powerup_sound = snd_arrow;

x += sprite_xoffset;
y += sprite_yoffset;